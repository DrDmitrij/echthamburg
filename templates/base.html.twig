<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.8.1/css/all.css" integrity="sha384-50oBUHEmvpQ+1lW4y57PTFmhCaXp0ML5d60M1M7uH2+nqUivzIebhndOJK28anvf" crossorigin="anonymous">
        {% endblock %}
    </head>
    <nav class="header">
        <div>
            <img src="../public/image/echt_logo.PNG" class="Firmen-Logo" title="Firmen Logo">
            <p class="username" style="float: right;
                                text-align: right;
                                margin: 0;
                                font-size: 12px;"
            ></p>
            {% if session == true %}
                <a href="{{ path('app_frontend_logout') }}"><p style="text-align: right;"><strong>Logout</strong></p></a>
            {% endif %}
        </div>
    </nav>
    {% block body %}
        {% block javascripts %}{% endblock %}
    {% endblock %}
    <nav class="footer">
        <div>
            <ul>
                <li>Impressum</li>
                <li>Datenschutz</li>
                <li>AGB</li>
            </ul>
            <p style="font-size: 12px; position: absolute; bottom: 0px">Copyright © 2019 Security Squad GmbH</p>
        </div>
    </nav>
    <style>
        .header { height: 60px; padding: 5px; background-color: white; box-shadow: 0 3px 6px rgba(0,0,0,.1); }
        .footer { min-height: 60px; padding: 5px; background-color: black; color: white; text-align: center; }
        .footer ul { list-style: none; padding: 0px; }
        .footer li { display: inline; margin: 0px 20px; }
        .body { background: #f2f2f2; margin: 0; font-family: NiveauGroteskRegular,Helvetica,Arial,sans-serif }
        .body { min-height: 60px; margin: 10px; padding: 5px; }
        .body .get_user_data input { margin: 10px 0px; }
        .body .get_user_data div { display: inline-block; }
        .body .user_overview { padding: 20px; background-color: white; box-shadow: 0px 0px 5px 3px rgba(0,0,0,.1); }
        .body .user_overview table { border-collapse: collapse; }
        .body .user_overview table th, td { border: 1px solid black; padding: 5px; }
        .body .user_overview table th { background-color: #0b5394; color: white; }
        .body .user_overview table button { margin: 5px; }
    </style>
</html>
